{
    "/": {
        "title": "{{ name }}",
        "description": "{{ name }}",
        "keywords": "{{ name }}"
    },
    "/principal": {
        "title": "{{ name }}",
        "description": "{{ name }}",
        "keywords": "{{ name }}"
    }
}
