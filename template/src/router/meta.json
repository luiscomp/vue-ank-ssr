{
    "/": {
        "title": "{{ name }}",
        "description": "{{ name }}",
        "keywords": "{{ name }}"
    },
    "/dashboard": {
        "title": "{{ name }}",
        "description": "{{ name }}",
        "keywords": "{{ name }}"
    }
}
